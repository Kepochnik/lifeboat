{
  "name": "lifeboat-pass-and-play",
  "version": "3.1.0",
  "private": true,
  "type": "module",
  "description": "A local pass-and-play companion for Lifeboat with an accessible bilingual interface.",
  "scripts": {
    "check": "node --check src/data.js && node --check src/i18n.js && node --check src/engine.js && node --check src/app.js && node --check sw.js",
    "test": "node --test",
    "verify": "npm run check && npm test"
  },
  "engines": {
    "node": ">=20"
  },
  "license": "MIT"
}
